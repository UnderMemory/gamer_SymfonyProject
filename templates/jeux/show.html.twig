{% extends 'base.html.twig' %}

{% block title %}Jeux{% endblock %}

{% block body %}
    <div class="show-top">
        <h1 class="list-jeux-title">{{ jeux.nom }}</h1>
        <div class="neon-bar mt-5"></div>
    </div>

    <div class="container">
        <table>
            <tr class="list-jeux-form d-flex flex-row">
                <td class="game-list w-25 my-5">{{ jeux.photo }}</td>
                <!--<td>{{ jeux.id }}</td>-->
                <td class="game-list w-25 my-5">{{ jeux.nom }}</td>
                <td class="game-list w-50 my-5 me-5">{{ jeux.description }}</td>
                <td class="game-list w-50 my-5 me-5">{{ note }}</td>

                <td class="game-list-liens my-5 d-flex flex-column">
                    <a class="game-list-lien" href="{{ path('jeux_edit', {'id': jeux.id}) }}">Modifier</a>
                    <a class="game-list-lien" href="{{ path('jeux_index') }}">Retour à la liste</a>
                </td>
            </tr>
        </table>

        {{ include('jeux/_delete_form.html.twig') }}
    </div>

{% endblock %}

{% block stylesheet %}
    {{ parent() }}
{% endblock %}