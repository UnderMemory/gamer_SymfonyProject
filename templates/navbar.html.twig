{% block navbar %}

	<nav class="navbar navbar-expand-lg navbar-light" style="background-color: lightcyan;">

	<div class="container-fluid">
		<a class="navbar-brand" href="#">Gamer</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse d-flex justify-content-center" id="navbarNav">

				<ul class="navbar-nav">
					<li class="nav-item mx-3">
						<a class="nav-link" href="{{path('index')}}">Accueil</a>
					</li>
					<li class="nav-item mx-3">
						<a class="nav-link" href="{{path('jeux_index')}}">Liste des jeux</a>
					</li>
					{% if is_granted('ROLE_USER') %}
						<li class="nav-item mx-3">
							<a class="nav-link" href="{{path('user_jeux')}}">Mes jeux</a>
						</li>
					{% endif %}
				</ul>
				<ul class="navbar-nav ml-auto">
					{% if is_granted('ROLE_USER') %}
						<span class='mr-5'>
							Bonjour {{app.user.pseudo }}
						</span>
						<a class="nav-link" href="{{path('app_logout')}}">Déconnection</a>
					{% else %}
						<li class="nav-item mx-3">
							<a class="nav-link" href="{{path('app_login')}}">Connection</a>
						</li>
						<li class="nav-item mx-3">
							<a class="nav-link" href="{{path('app_register')}}">Inscription</a>
						</li>
					{% endif %}
				</ul>
			</div>
	</div>


	</nav>

{% endblock %}

